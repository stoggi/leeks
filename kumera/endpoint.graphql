
type Endpoint {
    id: String!
    name: String!
}

input CreateEndpoint {
    name: String!
}

input UpdateEndpoint {
    id: String!
    name: String
}

type EndpointResult {
    endpoint: Endpoint
    error: String
}